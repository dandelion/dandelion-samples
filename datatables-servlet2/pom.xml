<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
   xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
   <modelVersion>4.0.0</modelVersion>
   
   <parent>
      <groupId>com.github.dandelion</groupId>
      <artifactId>dandelion-samples</artifactId>
      <version>0.8.4-SNAPSHOT</version>
   </parent>
   
   <groupId>com.github.dandelion</groupId>
   <artifactId>demo-datatables-servlet2</artifactId>
   <version>0.1.0-SNAPSHOT</version>
   <packaging>war</packaging>

   <name>Dandelion :: Samples :: Dandelion-datatables Servlet2</name>

   <description>Dandelion-datatables demo using the backward compatibility module for Servlet 2.x</description>

   <properties>
      <!-- Configuration -->
      <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

      <!-- Dependencies -->
      <datatables4j.version>0.8.4-SNAPSHOT</datatables4j.version>
      <datatables4j.servlet2.version>0.1.3-SNAPSHOT</datatables4j.servlet2.version>
      <spring.version>3.1.2.RELEASE</spring.version>
      <jstl.version>1.2</jstl.version>
      <servlet.version>2.5</servlet.version>
      <slf4j.version>1.6.4</slf4j.version>
      <logback.version>1.0.7</logback.version>

      <!-- Maven plugins -->
      <maven-compiler-plugin.version>2.3.2</maven-compiler-plugin.version>
   </properties>

   <dependencies>
      <!-- DataTables4j -->
      <dependency>
         <groupId>com.github.dandelion</groupId>
         <artifactId>datatables-jsp</artifactId>
         <version>${datatables4j.version}</version>
      </dependency>

      <dependency>
         <groupId>com.github.dandelion</groupId>
         <artifactId>datatables-servlet2</artifactId>
         <version>${datatables4j.servlet2.version}</version>
      </dependency>

      <!-- Spring -->
      <dependency>
         <groupId>org.springframework</groupId>
         <artifactId>spring-web</artifactId>
         <version>${spring.version}</version>
      </dependency>
      <dependency>
         <groupId>org.springframework</groupId>
         <artifactId>spring-webmvc</artifactId>
         <version>${spring.version}</version>
      </dependency>

      <!-- JSTL -->
      <dependency>
         <groupId>jstl</groupId>
         <artifactId>jstl</artifactId>
         <version>${jstl.version}</version>
      </dependency>

      <!-- Logging -->
      <dependency>
         <groupId>org.slf4j</groupId>
         <artifactId>slf4j-api</artifactId>
         <version>${slf4j.version}</version>
         <scope>compile</scope>
      </dependency>
      <dependency>
         <groupId>ch.qos.logback</groupId>
         <artifactId>logback-classic</artifactId>
         <version>${logback.version}</version>
      </dependency>
      <dependency>
         <groupId>ch.qos.logback</groupId>
         <artifactId>logback-access</artifactId>
         <version>${logback.version}</version>
      </dependency>
   </dependencies>

   <build>
      <plugins>
         <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-compiler-plugin</artifactId>
            <version>${maven-compiler-plugin.version}</version>
            <configuration>
               <source>1.6</source>
               <target>1.6</target>
            </configuration>
         </plugin>
         <plugin>
            <groupId>org.mortbay.jetty</groupId>
            <artifactId>jetty-maven-plugin</artifactId>
            <version>8.1.5.v20120716</version>
            <configuration>
               <scanIntervalSeconds>0</scanIntervalSeconds>
               <stopKey>jetty-stop</stopKey>
               <stopPort>9998</stopPort>
            </configuration>
         </plugin>
      </plugins>
   </build>
</project>